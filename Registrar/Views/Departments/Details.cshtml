@{
  Layout = "_Layout";
}

@model Registrar.Models.Department;

<h2>Department Details</h2>
<hr />
<h3>@Html.DisplayNameFor(model => model.DepartmentName): @Html.DisplayFor(model => model.DepartmentName)</h3>
<hr>

@if(@Model.Courses.Count == 0)
{
  <p>This department does not contain any courses</p>
}
else
{
  <h4>Courses this department contains:</h4>
  <ul>
  @foreach(var join in Model.Courses)
  {
    <li>@Html.ActionLink(@join.Course.CourseName, "Details", "Courses", new { id = join.CourseId })</li>
  }
  </ul>
}

<p>@Html.ActionLink("Add course to this department", "Create", "Courses")</p>
<p>@Html.ActionLink("Back to departments", "Index")</p>
<p>@Html.ActionLink("Edit department", "Edit", new { id = Model.DepartmentId })</p>
<p>@Html.ActionLink("Delete department", "Delete", new { id = Model.DepartmentId })</p>